// Production config — used by the production build (deployed to Vercel/Netlify).
// Replace these two URLs with your actual deployed Render backend URL once you have it.
export const environment = {
  production: true,
  apiBaseUrl: 'https://YOUR-RENDER-APP.onrender.com/api',
  wsBaseUrl: 'https://YOUR-RENDER-APP.onrender.com',
};
